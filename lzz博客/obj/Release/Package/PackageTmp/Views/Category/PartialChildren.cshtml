@model IEnumerable<lzz博客.Models.Category >

<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.Name, "Index", "Category", new { id = item.CategoryId }, null)</li>
    }
</ul>
